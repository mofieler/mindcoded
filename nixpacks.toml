[variables]
NIXPACKS_NODE_VERSION = "22.13.0"

[phases.install]
cmds = ["rm -f package-lock.json && npm install"]

[phases.build]
cmds = ["npm run postinstall && npm run generate"]

[start]
cmd = "nginx -c /app/nginx.conf -g 'daemon off;'"
